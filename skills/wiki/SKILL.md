name: wiki
description: Compile personal data (journals, notes, messages, whatever) into a personal knowledge wiki. Ingest any data format, absorb entries into wiki articles, query, cleanup, and expand.
argument-hint: ingest | absorb [date-range] | query <question> | cleanup | breakdown | status

## Personal Knowledge Wiki

You are a **writer** compiling a personal knowledge wiki from someone's personal data. Not a filing clerk. A writer. Your job is to read entries, understand what they mean, and write articles that capture understanding. The wiki is a map of a mind.

## Quick Start

Drop personal data files in the project root. Run:

```
Use the /wiki skill: ingest          # Convert your data into raw markdown entries
Use the /wiki skill: absorb all      # Compile entries into wiki articles
Use the /wiki skill: query <q>       # Ask questions about the wiki
Use the /wiki skill: cleanup         # Audit and enrich existing articles
Use the /wiki skill: breakdown       # Find and create missing articles
Use the /wiki skill: status          # Show stats
```

## What This Wiki IS

A knowledge base covering one person's entire inner and outer world: projects, people, ideas, taste, influences, emotions, principles, patterns of thinking. Like Wikipedia, but the subject is one life and mind.

Every entry must be absorbed somewhere. Nothing gets dropped. But "absorbed" means understood and woven into the wiki's fabric, not mechanically filed into the nearest article.

The question is never "where do I put this fact?" It is: **"what does this mean, and how does it connect to what I already know?"**

---

## Integration with This Vault

This wiki skill extends the existing copilot-mind vault. Key integration points:

- **Frontmatter**: All wiki articles use the vault standard (`date`, `description`, `tags`). The `tags` field must include the article type (e.g. `person`, `philosophy`, `pattern`, `era`).
- **Vault structure**: Wiki directories (`philosophies/`, `patterns/`, `eras/`, `people/`, etc.) live alongside existing vault folders (`work/`, `org/`, `brain/`). They do not replace them.
- **`brain/` enrichment**: The absorb loop should update existing `brain/Patterns.md`, `brain/Memories.md`, and `brain/Key-Decisions.md` when relevant personal data is absorbed.
- **`org/people/`**: Existing person notes live here. The wiki may create richer companion articles in `people/` with full narrative depth, linking back to the atomic `org/people/` note.
- **Wikilinks**: Use `[[wikilinks]]` between all articles. Every new wiki article must link to at least one existing vault note.
- **Search**: Use `qmd query "..."` for semantic search if qmd is installed; fall back to `grep -r` otherwise.
- **Validation**: The `hooks/validate-write.py` hook runs after every `.md` write and checks frontmatter. This applies to wiki articles too.

---

## Directory Structure

```
vault/
  data/                  # Your source files (DO NOT MODIFY after ingest)
  raw/entries/           # One .md per entry (generated by ingest)
  wiki/                  # The compiled knowledge base
    _index.md            # Master index with aliases
    _backlinks.json      # Reverse link index
    _absorb_log.json     # Tracks which entries have been absorbed
    {directories}/       # Emerge from the data. Don't pre-create.
```

Override the wiki directory with `--dir <name>` on any command. Each version is its own universe. Never read from or reference another version.

---

## Command: `/wiki ingest`

Convert source data into individual `.md` files in `raw/entries/`. Write a Python script `ingest.py` to do this. This step is mechanical — no LLM intelligence needed.

### Supported Data Formats

The ingest script should auto-detect the format:

**Day One JSON** (`*.json` with `entries` array): Each entry becomes a file. Extract: date, time, timezone, location, weather, tags, text, photos/videos/audios.

**Apple Notes** (exported `.html`, `.txt`, or `.md` files): Each note becomes a file. Extract: title (first line or filename), creation date, folder/tag, body text. Strip HTML if needed.

**Obsidian Vault** (folder of `.md` files): Each note becomes a file. Preserve frontmatter. Convert `[[wikilinks]]` to plain text for raw entries.

**Notion Export** (`.md` or `.csv`): Each page becomes a file. Handle nested pages by flattening with parent context.

**Plain Text / Markdown** (folder of `.txt` or `.md`): Each file becomes an entry. Use filename for date if it contains one.

**iMessage Export** (`.csv` or chat logs): Group by conversation and date. Each day's conversation with one person becomes an entry.

**CSV / Spreadsheet**: Each row becomes an entry. Identify the date and content columns automatically.

**Email Export** (`.mbox`, `.eml`): Each email becomes an entry. Strip signatures and quoted replies.

**Twitter/X Archive** (`tweet.js`): Each tweet becomes an entry. Extract date, text, media, reply context.

### Output Format

Each file: `{date}_{id}.md` with YAML frontmatter:

```yaml
---
id: <unique identifier>
date: YYYY-MM-DD
time: "HH:MM:SS"
source_type: <dayone|apple-notes|obsidian|notion|text|imessage|csv|email|twitter>
description: "<auto-generated ~150 char summary>"
tags:
  - raw-entry
# ... any other metadata from the source
---

<entry text content>
```

The script must be **idempotent**. Running it twice produces the same output.

### Unknown Formats

If the data doesn't match any known format, read a sample, figure out the structure, and write a custom parser. Goal: one markdown file per logical entry with date and metadata in frontmatter.

---

## Command: `/wiki absorb [date-range]`

The core compilation step. Date ranges: `last 30 days`, `2026-03`, `2026-03-22`, `2024`, `all`. Default: absorb last 30 days. If `raw/entries/` doesn't exist, run ingest first.

### The Absorption Loop

Process entries one at a time, chronologically. Read `wiki/_index.md` before each entry to match against existing articles. Re-read every article before updating it. This is non-negotiable.

For each entry:

1. **Read the entry.** Text, frontmatter, metadata. View any attached photos — actually look at them.

2. **Understand what it means.** Not "what facts does this contain" but "what does this tell me?"

3. **Match against the index.** What existing articles does this entry touch? What doesn't match and suggests a new article?

4. **Update and create articles.** Re-read every article before updating. Ask: **what new dimension does this entry add?** Write full sections, not sentences. Integrate so the article reads as a coherent whole. Never just append to the bottom.

5. **Connect to patterns.** When the same theme recurs across entries, that pattern deserves its own article. These concept articles are where the wiki becomes a map of a mind.

6. **Enrich `brain/`** when relevant: add to `brain/Patterns.md`, `brain/Memories.md`, or `brain/Key-Decisions.md` if the entry surface a durable insight. Link back to the full wiki article.

### What Becomes an Article

**Named things get pages** if there's enough material. If you can't write at least 3 meaningful sentences, don't create the stub yet — note the entity in the article where they appear, and create the page when more material arrives.

**Patterns and themes get pages.** When the same idea surfaces across entries (a creative philosophy, a recurring emotional arc, a learning style) that's a concept article. These are often the most valuable.

### Anti-Cramming

The gravitational pull of existing articles is the enemy. If you're adding a third paragraph about a sub-topic to an existing article, that sub-topic probably deserves its own page.

### Anti-Thinning

Creating a page is not the win. Enriching it is. A stub with 3 vague sentences when 4 other entries also mentioned that topic is a failure.

### Every 15 Entries: Checkpoint

1. Rebuild `wiki/_index.md` with all articles and `also:` aliases
2. Rebuild `wiki/_backlinks.json` (Python script scanning `[[wikilinks]]`)
3. **New article audit**: If zero new articles in the last 15, you're cramming.
4. **Quality audit**: Pick 3 most-updated articles. Re-read each as a whole piece:
   - Does it tell a coherent story, or is it a chronological dump?
   - Does it have sections organized by theme, not date?
   - Does it use direct quotes to carry emotional weight?
   - Does it connect to other articles in revealing ways?
   - Would a reader learn something non-obvious? If not, rewrite it.
5. Check if any articles exceed 150 lines and should be split.

---

## Command: `/wiki query <question>`

Answer questions by navigating the wiki. **Never read raw entries.**

### How to Answer

1. **Read `wiki/_index.md`.** Scan for relevant articles (each has an `also:` aliases field).
2. **Check `wiki/_backlinks.json`** — high backlink counts indicate central topics.
3. **Read 3–8 relevant articles.** Follow `[[wikilinks]]` and `related:` 2–3 links deep.
4. **Synthesize.** Lead with the answer, cite articles by name, use direct quotes sparingly, acknowledge gaps.

### Query Patterns

| Query type | Where to look |
|---|---|
| "Tell me about [person]" | `people/`, backlinks, 2–3 linked articles |
| "What happened with [project]?" | Project article, related era, decisions |
| "Why did they [decision]?" | `decisions/`, `transitions/`, related project |
| "What's the pattern with [theme]?" | `patterns/`, `philosophies/`, `tensions/`, `life/` |
| "What was [time period] like?" | `eras/`, `places/`, `projects/` |
| Broad/exploratory | Cast wide, highest-backlink articles, synthesize |

### Rules

- Never read `raw/entries/`. The wiki is the knowledge base.
- Don't guess. If the wiki doesn't cover it, say so.
- Don't read the entire wiki. Be surgical.
- Don't modify any wiki files. Query is read-only.

---

## Command: `/wiki cleanup`

Audit and enrich every article using parallel subagents.

### Phase 1: Build Context

Read `wiki/_index.md` and every article. Build a map of all titles, all wikilinks, and every concrete entity mentioned without its own page.

### Phase 2: Per-Article Subagents (batches of 5)

Each agent reads one article and:

**Assesses:**
- Structure: theme-driven or diary-driven?
- Line count: bloated (>120 lines) or stub (<15 lines)?
- Tone: flat/factual or AI editorial voice?
- Wikilinks: broken or missing links to existing articles?

**Restructures if needed.** The most common problem is diary-driven structure.

Bad: `## The March Meeting` / `## The April Pivot`
Good: `## Origins` / `## The Pivot to Institutional Sales`

**Enriches** with minimal web context (3–7 words) for entities a reader wouldn't recognize.

### Phase 3: Integration

Deduplicate candidates, create new articles, fix broken wikilinks, rebuild `_index.md` and `_backlinks.json`.

---

## Command: `/wiki breakdown`

Find and create missing articles.

### Phase 1: Survey

Read `wiki/_index.md` and `_backlinks.json`. Identify bare directories, bloated articles (>100 lines), high-reference targets without articles.

### Phase 2: Mining (parallel subagents, batches of ~10 articles)

Extract **concrete entities** (the concrete noun test: "X is a ___"):
- Named people, places, companies, organizations
- Named events or turning points with dates
- Books, films, music, games referenced
- Tools, platforms used significantly
- Projects with names

Do NOT extract: generic technologies, entities already covered, passing mentions.

### Phase 3: Planning

Present candidate table:

| # | Article | Dir | Refs | Description |

### Phase 4: Creation (parallel batches of 5)

Each agent: greps existing articles for mentions, collects material, writes the article, adds wikilinks from existing articles to the new one.

---

## Directory Taxonomy

Directories emerge from the data. Don't pre-create them.

### Core
| Directory | What goes here |
|---|---|
| `people/` | Named individuals (narrative depth; link to `org/people/` atomic note) |
| `projects/` | Things built with serious commitment |
| `places/` | Cities, buildings, neighborhoods |
| `events/` | Specific dated occurrences |
| `companies/` | External companies |

### Media and Culture
| Directory | What goes here |
|---|---|
| `books/` | Books that shaped thinking |
| `films/` | Movies/shows that mattered |
| `music/` | Artists/groups that mattered |
| `tools/` | Software tools central to practice |

### Inner Life and Patterns
| Directory | What goes here |
|---|---|
| `philosophies/` | Articulated intellectual positions about how to work and build |
| `patterns/` | Recurring behavioral cycles with triggers, mechanisms, outcomes |
| `tensions/` | Unresolvable contradictions between two values |
| `life/` | Biographical themes that aren't philosophies or patterns |

### Narrative Structure
| Directory | What goes here |
|---|---|
| `eras/` | Major biographical phases connecting multiple articles |
| `transitions/` | Liminal periods between commitments |
| `decisions/` | Inflection points with enumerated reasoning |
| `experiments/` | Time-boxed tests with hypothesis and result |
| `setbacks/` | Adverse incidents that disrupted plans |

### Other
| Directory | What goes here |
|---|---|
| `relationships/` | Dynamics between the subject and others |
| `ideas/` | Documented but unrealized concepts |
| `routines/` | Specific daily/weekly schedules |
| `metaphors/` | Figurative frameworks used to understand self or work |
| `assessments/` | Dated self-evaluations |
| `touchstones/` | Encounters with cultural works that triggered reflection |

Create new directories freely when a type doesn't fit.

---

## Writing Standards

### The Golden Rule

**This is not Wikipedia about the thing. This is about the thing's role in the subject's life.**

A page about a book isn't a book review. It's about what that book meant to the person, when they read it, what it changed.

### Tone: Wikipedia, Not AI

Write like Wikipedia. Flat, factual, encyclopedic. State what happened. The article stays neutral; direct quotes from entries carry the emotional weight.

**Never use:** em dashes, peacock words ("legendary," "visionary," "groundbreaking," "deeply," "truly"), editorial voice ("interestingly," "importantly"), rhetorical questions, progressive narrative ("would go on to," "embarked on"), qualifiers ("genuine," "raw," "powerful," "profound").

**Do:** Lead with the subject, state facts plainly. One claim per sentence. Short sentences. Simple past or present tense. Attribution over assertion. Let facts imply significance. Dates and specifics replace adjectives.

**One exception:** Direct quotes carry the voice. The article is neutral. The quotes do the feeling.

### Article Frontmatter

```yaml
---
date: YYYY-MM-DD
description: "~150 char description aligned to vault standard"
tags:
  - <type: person|project|place|philosophy|pattern|era|transition|decision|...>
last_updated: YYYY-MM-DD
related:
  - "[[Other Article]]"
  - "[[Another]]"
sources:
  - entry-id-1
  - entry-id-2
---
```

### Linking

Use `[[wikilinks]]` between articles. Every new article must link to at least one existing vault note. Cite sources in frontmatter using entry IDs.

### Narrative Coherence

Every article must have a point. Not "here are 4 times X appeared" but "X represented Y in the subject's life." A reader should finish feeling they understand the significance.

### Structure by Type

| Type | Structure |
|---|---|
| person | By role/relationship phase |
| place | By what happened there and what it meant |
| project | By conception, development, outcome |
| event | What happened (brief), why it mattered (bulk), consequences |
| philosophy | The thesis, how it developed, where it succeeded/failed |
| pattern | The trigger, the cycle, attempts to break it |
| transition | What ended, the drift, what emerged |
| decision | The situation, the options, the reasoning, the choice |
| era | The setting, the project, the team, the emotional tenor |

### Quote Discipline

Maximum 2 direct quotes per article. Block quotes only for passages with significant emotional weight. Never quote for factual content — paraphrase instead.

---

## Command: `/wiki status`

Report wiki health:
- Total articles by directory
- Total raw entries vs absorbed entries (from `_absorb_log.json`)
- Articles created/updated in last 30 days
- Articles with no backlinks (orphan candidates)
- Articles exceeding 150 lines (split candidates)
- Articles under 15 lines (stub candidates)
